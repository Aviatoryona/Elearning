<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <item
        android:id="@+id/student"
        android:title="Student Only"
        tools:ignore="HardcodedText" />

    <item
        android:id="@+id/tutor"
        android:title="Tutor Only"
        tools:ignore="HardcodedText" />

    <item
        android:id="@+id/admin_only"
        android:title="Admin Only"
        tools:ignore="HardcodedText" />

    <item
        android:id="@+id/admin_tutor"
        android:title="Admin, Tutor"
        tools:ignore="HardcodedText" />
    <item
        android:id="@+id/grant_all"
        android:title="Grant All"
        tools:ignore="HardcodedText" />
</menu>